<form role="form" ng-controller="NewReleaseBlockedController">
    <div ng-if="walletId">
        <div class="row">
            <div class="col-lg-12">
                <div class="form-group">
                    <label>{{ 'Amount'|trans }} <span class="text-danger">*</span></label>
                    <input type="number" step="0.01" placeholder="0" class="form-control" name="amount" ng-model="releaseBlocked.amount">
                </div>
            </div>
        </div>
        <div>
            <button ng-click="makeReleaseBlocked()" class="btn btn-success btn-outline btn-sm" type="button"><i class="fa fa-check"></i> {{ 'Release'|trans }}</button>
            <div>
                <small class="text-danger float-right">* {{ 'required'|trans }}</small>
            </div>
        </div>
    </div>
    <div ng-if="!walletId" class="text-center text-muted">
        {{ 'Release not allowed. No wallet selected.'|trans }}
    </div>
</form>